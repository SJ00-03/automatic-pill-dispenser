# PROJECT_ANALYSIS

## 1) 프로젝트 한 줄 요약
- **코드 확인:** STM32F411 기반 임베디드 자동 약 디스펜서 펌웨어로, RTC 알람/블루투스(UART)/초음파 센서/모터/부저/LCD를 연동해 알약 배출과 사용자 안내를 수행합니다.

## 2) 프로젝트 목적
- **코드 확인:** 정해진 시간(RTC Alarm)에 맞춰 약통 슬롯을 회전시키고, 서보를 통해 배출 동작을 수행한 뒤, 컨베이어와 거리 센서 기반 상태 머신으로 사용자 수령 절차(정지/알림/복귀)를 자동화하는 것이 목적입니다.
- **코드 확인:** 스마트폰(BLE UART1) 명령으로 현재 시간 동기화, 알람 시간 설정, 7일 자동 채우기 패턴 전송을 지원합니다.
- **추정:** 복약 순응도 향상(정시 복용 유도)과 보호자/사용자 편의성 개선을 제품 목표로 둔 프로젝트입니다.

## 3) 전체 시스템 구조
- **코드 확인:** `main` 루프 중심의 bare-metal 구조이며, 드라이버 계층(`drivers/`) + 앱 로직(`src/app/`) + 시스템 초기화/예외(`system/`, `startup/`)로 분리되어 있습니다.
- **코드 확인:** 입출력 장치 구성
  - 입력: BLE/UART1 명령, UART2(PC 디버그/수동 트리거), 초음파 센서, 외부 키
  - 출력: DC 모터, 스텝 모터 2축, 서보 모터, 부저, 상태 LED, 2개 I2C LCD
- **추정:** 앱(모바일) ↔ BLE 모듈(UART1) ↔ MCU 간 단순 텍스트 프로토콜 기반 제어 구조입니다.

## 4) 주요 폴더/파일 설명
- `src/app/main.c`: 시스템 초기화, 메인 제어 루프, 상태 머신, 자동 채우기 시퀀스.
- `src/app/alarm.c`: RTC 초기화/현재 시간 설정/알람 설정.
- `drivers/uart.c`: UART1/2 초기화, 송수신, UART1 인터럽트 파서(T/A/L 명령).
- `drivers/motor.c`: DC 모터 PWM, 서보 제어, 스텝 모터 슬롯 회전/공급 제어.
- `drivers/ultrasonic.c`: 초음파 트리거/에코 시간 측정 기반 거리 계산.
- `drivers/lcd.c`, `src/app/lcd_status.c`: LCD 출력, 상태 표시.
- `include/device_driver.h`: 모듈 간 외부 함수 인터페이스 집약 헤더.
- `system/*`, `startup/crt0.s`: 클럭/런타임/예외/부팅 코드.
- `Makefile`: ARM GCC 크로스 컴파일, 링크, 바이너리/덤프 생성, 플래시 규칙.

## 5) 실행 흐름
1. **부팅/초기화**: 클럭, UART1/2, LED, 모터, LCD 2개, 초음파, 키, 부저, UART RX 인터럽트 활성화.
2. **RTC 설정**: 기본 알람 초기 설정.
3. **무한 루프 동작**
   - UART 명령 처리(현재 구현상 `Process_UART_Input`은 비어 있고, 실제 BLE 명령은 USART1 IRQ에서 파싱).
   - 자동 채우기 플래그 확인 시 7일 시퀀스 실행.
   - 현재 시각/알람 시각을 주기적으로 LCD/시리얼 출력.
   - 알람 발생 플래그 시 슬롯 회전 + 배출 + 컨베이어 시작.
   - 초음파 거리 기반 상태 머신:
     - FORWARD: 근접 감지 시 정지 + 부저 ON + WAIT
     - WAIT: 사용자 확인 입력 후 부저 OFF + 역회전(BACKWARD)
     - BACKWARD: 복귀 완료 조건 후 정지(FINISHED)
   - IDLE/FINISHED에서는 UART2 수동 트리거(`f/F`)로 수동 배출 가능.
- **추정:** WAIT→BACKWARD 전환의 사용자 확인 입력은 외부 스위치(키) 또는 UART 입력 기반으로 설계됐을 가능성이 큽니다.

## 6) 핵심 모듈 설명
- **스케줄/시간 모듈(RTC)**: LSE(32.768kHz) 기반 RTC 구성, 알람 인터럽트 활성화, 시간/알람 동적 갱신.
- **통신 모듈(UART)**: 
  - UART1: BLE/앱 제어 채널(인터럽트 기반 수신).
  - UART2: PC 디버그 및 수동 테스트 채널.
- **구동 모듈(Motor)**:
  - DC 모터(TIM5 PWM)로 컨베이어 구동.
  - 서보(TIM3 PWM)로 배출구 개폐.
  - 스텝 모터 2축으로 슬롯 인덱싱/자동 채우기 공급.
- **감지/알림 모듈**: 초음파 거리 측정 + 부저 + 상태 LED로 사용자 인터랙션 구현.
- **표시 모듈(LCD)**: 2개 주소(0x4E, 0x4C) LCD에 시계/알람/로딩 상태 표시.

## 7) 사용 기술 스택
- **코드 확인:** C(gnu99), ARM Cortex-M4(stm32f411), CMSIS, bare-metal 레지스터 직접 제어.
- **코드 확인:** 주변장치: UART, RTC, TIM(PWM/딜레이), GPIO, EXTI/NVIC, I2C(LCD), 초음파 센서 인터페이스.
- **코드 확인:** 빌드: `arm-none-eabi-gcc` 기반 Makefile, linker script 사용.
- **추정:** BLE 모듈은 UART 브리지형(HM-10/HC-05 계열 유사)으로 동작합니다.

## 8) 코드에서 확인되는 주요 기능
- RTC 현재 시각 출력 및 알람 시각 관리.
- BLE 텍스트 명령 처리:
  - `T hh:mm:ss` 현재 시간 동기화
  - `A hh:mm:ss` 알람 설정
  - `L1010100` 형태 7일 자동 채우기 패턴 설정
- 알람 시 약 배출 시퀀스(슬롯 회전 + 서보 개폐).
- 자동 채우기 시퀀스(7칸 순회, 조건부 공급, 다음 요일 칸 이동).
- 초음파 거리 감지에 따른 컨베이어 정지/대기/복귀 상태 제어.
- 부저/LED/LCD를 통한 상태 안내.
- UART2 수동 트리거 기반 디버그 동작.

## 9) 포트폴리오에 쓸 수 있는 기술 역량
- **임베디드 시스템 통합 능력:** RTC, UART, PWM, GPIO, 인터럽트, 센서, 액추에이터를 단일 펌웨어로 통합.
- **실시간 제어 로직 설계:** 상태 머신 기반 시퀀스 제어(IDLE/FORWARD/WAIT/BACKWARD/FINISHED).
- **저수준 디버깅/검증 경험:** 시리얼 로그, 수동 트리거, 인터럽트 기반 명령 파싱.
- **하드웨어 제약 대응:** 스텝 수/딜레이 튜닝, 안전 정지/복귀 동작 설계.
- **실무형 표현 예시:** “사용자 이벤트와 센서 이벤트를 결합한 복약 디스펜서 제어 펌웨어를 설계/구현하고, UART 명령 프로토콜과 RTC 알람 기반 자동 배출 플로우를 구현함.”

## 10) 면접 예상 질문과 답변 포인트
1. **Q. 왜 인터럽트(UART1)와 폴링(메인 루프)을 혼합했나요?**
   - A 포인트: BLE 명령은 비동기 입력이라 인터럽트가 유리하고, 모터/센서 상태 관리는 주기 루프에서 상태 머신으로 단순/명확하게 유지.
2. **Q. 상태 머신 설계 시 핵심 고려사항은?**
   - A 포인트: 오작동 시 안전 정지, 사용자 확인 단계(WAIT), 복귀(BACKWARD) 분리, 디버그 가능성.
3. **Q. RTC 알람 신뢰성은 어떻게 확보했나요?**
   - A 포인트: LSE 사용, BCD 변환 일관성, 알람 레지스터 write-protect 절차 준수.
4. **Q. 모터 제어에서 PWM/스텝 제어를 분리한 이유는?**
   - A 포인트: DC 모터는 속도/방향 제어, 스텝 모터는 각도/인덱싱 정확도 목적이 달라 제어 방식 분리.
5. **Q. 개선하고 싶은 지점은?**
   - A 포인트: 명령 파서 모듈화, 센서 필터링/디바운싱, 에러 코드 체계화, 단위 테스트 가능한 계층 분리.

## 11) 코드만으로는 확인할 수 없는 정보 목록
- 실제 하드웨어 배선도/핀맵 문서(모터 드라이버, 초음파, LCD, BLE 모듈 정확 모델).
- 기구 설계 정보(약통 슬롯 각도, 기어비 확정 근거, 복귀 기준 위치 센싱 방식).
- 모바일 앱 상세(플랫폼, 화면, 통신 재시도/에러 처리 정책).
- 전원/배터리 사양, 안전 규격 고려, 장시간 신뢰성 테스트 결과.
- 팀 개발 여부, 본인 기여 범위, 성능 지표(배출 성공률/오차/응답시간).

## 12) README 또는 포트폴리오 작성을 위해 추가로 필요한 정보
- **필수:** 보드/센서/모터/드라이버 부품 리스트(BOM)와 정확 모델명.
- **필수:** 핀맵 표(GPIO/타이머/UART/I2C/EXTI 매핑).
- **필수:** 빌드/플래시 절차(툴체인 경로 가변화, OS별 명령).
- **필수:** 명령 프로토콜 명세(T/A/L 포맷, 응답 코드, 오류 케이스).
- **권장:** 상태 머신 전이도, 시퀀스 다이어그램(알람 발생~복귀).
- **권장:** 트러블슈팅 섹션(센서 타임아웃, 모터 미동작, RTC 초기화 실패).
- **권장:** 검증 결과(테스트 케이스, 성공 기준, 한계 및 향후 개선).
- **추정:** 포트폴리오용으로는 “문제-해결-지표” 구조(예: 오배출 감소, 동작 안정화 시간 단축)를 정량 데이터와 함께 제시하면 설득력이 높습니다.
